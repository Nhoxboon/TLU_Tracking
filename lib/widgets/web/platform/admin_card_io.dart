// Platform-specific implementation for mobile/desktop (non-web)

void redirectToLogin() {
  // For mobile/desktop, we can't directly modify location.href
  // Instead, we'll handle this in the widget's code using Navigator
}
